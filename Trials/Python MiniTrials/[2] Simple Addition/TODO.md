# TODO: Simple Addition

Write a Python script that adds two numbers together and prints the result.

## Requirements
- Define two integer variables, `a = 5` and `b = 3`
- Compute their sum and store it in a variable called `result`
- Print the result in the format: `5 + 3 = 8`
