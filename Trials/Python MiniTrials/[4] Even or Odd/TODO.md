# TODO: Even or Odd

Write a Python script that determines whether a given number is even or odd.

## Requirements
- Ask the user to enter an integer with the prompt: `Enter a number: `
- Convert the input to an integer
- Use the modulo operator `%` to check if the number is even or odd
- Print either `<number> is even.` or `<number> is odd.`
