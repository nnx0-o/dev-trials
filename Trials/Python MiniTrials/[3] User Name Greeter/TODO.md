# TODO: User Name Greeter

Write a Python script that asks the user for their name and greets them.

## Requirements
- Use `input()` to prompt the user: `Enter your name: `
- Store the name in a variable
- Print a greeting in the format: `Hello, <name>! Nice to meet you.`
